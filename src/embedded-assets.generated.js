// Placeholder — replaced by `npm run prepare-assets`.
// With this empty version, `npm run dev` still works (it just fetches the
// model from IMG.LY's CDN over the network like the library normally does).
// Only `npm run build`'s offline output needs the real generated data.
export const EMBEDDED_MODEL = null;
export const EMBEDDED_ASSETS = {};
